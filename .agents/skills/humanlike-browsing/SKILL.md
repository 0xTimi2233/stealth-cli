---
name: humanlike-browsing
description: 驱动无头浏览器以会话隔离与指纹环境调度穿透安全质询并完成高保真页面操作；当需要多会话并发浏览、穿透安全质询、持久化账号操作或执行表单交互闭环时使用
---

驱动无头浏览器完成环境调度、会话隔离与拟人化交互，通过 UI 状态闭环穿透安全质询并达成业务终态

## 流程

### 1. 会话筹备

声明独立任务会话名以隔离并发进程：
每次启动必须指定显式会话标识（如 `--session worker-<task_id>`），底层默认分配独立随机硬件指纹与临时沙箱运行。若任务要求复用特定账号或维护登录态，按需查阅 [persistent-profiles.md](./references/persistent-profiles.md)

### 2. 页面触达

优先通过原生 `agent_browser` 工具携带会话参数执行 `open <url>` 导航至目标路由，随后执行 `snapshot -i` 获取首屏可交互元素快照

### 3. 状态解离

观察操作按钮状态与页面可用性：
- 操作按钮处于启用状态或已呈现目标业务视图：进入后续步骤
- 操作按钮处于禁用状态且存在安全质询：优先执行轻量复选框自愈；若遇字符扭曲验证码、滑块或复杂质询，按需查阅 [captcha-handling.md](./references/captcha-handling.md) 执行专项解离与阻断判定

### 4. 业务交互

若任务仅需读取或提取页面数据，直接解析快照内容并进入终态闭环；若需执行表单输入与提交：
- 文本输入：优先执行 `fill <input_ref> "<text>"` 原子填入；若页面有特殊按键监听，执行 `click <input_ref>` 聚焦后执行 `keyboard type "<text>"`
- 确认派发：执行 `press Enter` 或 `click <submit_ref>` 派发提交

### 5. 终态闭环

通过终态事实确认操作结果并释放资源：
- 成功闭环：执行 `get url` 或 `snapshot -i` 确认已跳转至目标路由或展示成功态元素
- 失败阻断：停留原页面或展示明确错误提示时，提取具体错误文字回传
- 会话释放：操作结束后统一执行 `close` 释放当前浏览器会话进程
